{
  "version": "2.0.0",
  "configurations": [

      {
          "name": "Debug",
          "type": "cppdbg",
          "request": "launch",
          "program": "${workspaceFolder}/debug/bin/beginner-opt",
          "cwd": "${workspaceRoot}",
          "preLaunchTask": "build"
      }
  ]
}
